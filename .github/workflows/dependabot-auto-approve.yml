name: Dependabot Auto-merge
on:
  pull_request:
    branches:
      - 'main'

permissions:
  pull-requests: write
  contents: write
  id-token: write

jobs:
  dependabot-auto-merge:
    permissions:
      id-token: write
      pull-requests: write
      contents: write
      actions: read
    if: github.event.pull_request.user.login == 'dependabot[bot]'
    uses: HillwoodPark/shared-github-workflows/.github/workflows/dependabot-auto-approve.yml@38f09831657ec33b9c6e77c752df17dc572b2e3f
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
    with:
      node-version: '24.x'
      npm-registry-url: 'https://npm.pkg.github.com'
