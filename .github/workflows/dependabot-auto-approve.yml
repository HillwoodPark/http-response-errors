name: Dependabot Auto-merge
on:
  pull_request:
    branches:
      - 'main'

permissions:
  pull-requests: write
  contents: write

jobs:
  dependabot-auto-merge:
    permissions:
      id-token: write
      pull-requests: write
      contents: write
      actions: read
    if: github.event.pull_request.user.login == 'dependabot[bot]'
    uses: HillwoodPark/shared-github-workflows/.github/workflows/dependabot-auto-approve.yml@634a7510b6eb224abb40a72e7ac675aff1feb70d
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
    with:
      node-version: '24.x'
      npm-registry-url: 'https://npm.pkg.github.com'
